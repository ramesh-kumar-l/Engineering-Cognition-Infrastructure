# ============================================================
# ECI — Development / CI dependencies
# Install in addition to requirements.txt for local dev and CI.
#
# Usage:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# uv users: dev deps are already declared in the root
# [dependency-groups].dev section — just run:
#   uv sync --all-packages
# ============================================================

# Linting + formatting
ruff>=0.6.0

# Static type checking
mypy>=1.11.0
types-requests

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0            # TestClient for FastAPI

# Documentation site
mkdocs-material>=9.5.0

# Security auditing
pip-audit>=2.7.0
bandit[toml]>=1.8.0
